# ============================================
# ARCHIVO DE CONFIGURACIÓN - TEMPLATE
# ============================================
# Este archivo es un TEMPLATE de ejemplo.
# Para usar este proyecto:
# 1. Copia este archivo y renómbralo a .env
# 2. Reemplaza todos los valores de ejemplo con tus valores reales
# 3. NUNCA subas el archivo .env a Git (ya está en .gitignore)
# ============================================

# ============================================
# CONFIGURACIÓN DE DJANGO
# ============================================
# Clave secreta de Django - Genera una nueva con: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=reemplaza-con-tu-secret-key-generada

# Modo debug (True para desarrollo, False para producción)
DEBUG=True

# Hosts permitidos (separados por comas, sin espacios)
ALLOWED_HOSTS=localhost,127.0.0.1

# ============================================
# CONFIGURACIÓN DE BASE DE DATOS POSTGRESQL
# ============================================
# Nombre de la base de datos
DB_NAME=postgres

# Usuario de la base de datos
DB_USER=postgres

# Contraseña de la base de datos
DB_PASSWORD=reemplaza-con-tu-password-de-base-de-datos

# Host de la base de datos (ejemplo: db.xxxxx.supabase.co)
DB_HOST=reemplaza-con-tu-host-de-base-de-datos

# Puerto de la base de datos (generalmente 5432 para PostgreSQL)
DB_PORT=5432

# ============================================
# CONFIGURACIÓN DE EMAIL (SMTP)
# ============================================
# Servidor SMTP (ejemplo: smtp.gmail.com)
EMAIL_HOST=smtp.gmail.com

# Puerto SMTP (587 para TLS, 465 para SSL)
EMAIL_PORT=587

# Usar TLS (True/False)
EMAIL_USE_TLS=True

# Email desde el cual se enviarán los correos
EMAIL_HOST_USER=reemplaza-con-tu-email@gmail.com

# Contraseña de aplicación del email (NO uses tu contraseña normal de Gmail)
# Para Gmail: Ve a tu cuenta > Seguridad > Contraseñas de aplicaciones
EMAIL_HOST_PASSWORD=reemplaza-con-tu-app-password

# ============================================
# NOTAS IMPORTANTES
# ============================================
# - Este archivo (.env.example) SÍ se sube a Git como template
# - El archivo .env (con valores reales) NO se sube a Git
# - Cada desarrollador debe crear su propio .env local
# - En producción, configura las variables de entorno en el servidor